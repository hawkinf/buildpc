@page "/orcamentos"
@attribute [Authorize]
@inject NavigationManager Navigation

@* A tela de orcamentos foi incorporada a Montagem: montar e consultar
   passaram a ser a mesma tela. A rota continua existindo so para nao
   quebrar link salvo, favorito ou PDF antigo que aponte para ca. *@

<PageTitle>Orçamentos - BuildPC</PageTitle>

<p>Redirecionando para a Montagem...</p>

@code {
    protected override void OnInitialized() =>
        Navigation.NavigateTo("montagem", replace: true);
}
